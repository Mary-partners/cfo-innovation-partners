import { PlaceholderPage } from "@/components/os/placeholder";

export default function WorkPage() {
  return (
    <PlaceholderPage
      title="Work"
      phase="Phase 1"
      description="Workflow templates, recurring jobs, tasks, checklists and dependencies for every engagement — the workflow engine described in /docs/implementation-plan.md."
    />
  );
}
